# Legacy Local Gate

The current shell-first CLI remains available for local smoke testing and backward compatibility.
New runtime enforcement code should use `src/action`, `src/roles`, and `src/core/policy`.

